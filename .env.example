# z.ai / GLM-5.1 credentials
ZAI_API_KEY=your-api-key-here
ZAI_BASE_URL=https://api.z.ai/api/paas/v4
ZAI_MODEL=glm-5.1

# Optional: cap output length on the LLM side (renderer also enforces its own cap)
ZAI_MAX_TOKENS=400
